/**
 * Options used to create a new Mailosaur server.
 */
declare class ServerCreateOptions {
    /**
     * A name used to identify the server.
     */
    name?: string;
    constructor(name?: string);
}
export default ServerCreateOptions;
