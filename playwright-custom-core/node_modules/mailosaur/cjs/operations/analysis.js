"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const spamAnalysisResult_js_1 = __importDefault(require("../models/spamAnalysisResult.js"));
const deliverabilityReport_js_1 = __importDefault(require("../models/deliverabilityReport.js"));
/**
 * Message analysis operations.
 */
class Analysis {
    constructor(client) {
        this.client = client;
    }
    /**
     * Perform a spam analysis of an email.
     * @param messageId The identifier of the message to be analyzed.
     */
    async spam(messageId) {
        const url = `api/analysis/spam/${messageId}`;
        return new Promise((resolve, reject) => {
            this.client.request.get(url, {}, (err, response, body) => {
                if (err) {
                    return reject(err);
                }
                if (!response || response.statusCode !== 200) {
                    return reject(response
                        ? this.client.httpError(response)
                        : new Error('No response received'));
                }
                resolve(new spamAnalysisResult_js_1.default(body));
            });
        });
    }
    /**
     * Perform a deliverability report of an email.
     * @param messageId The identifier of the message to be analyzed.
     */
    async deliverability(messageId) {
        const url = `api/analysis/deliverability/${messageId}`;
        return new Promise((resolve, reject) => {
            this.client.request.get(url, {}, (err, response, body) => {
                if (err) {
                    return reject(err);
                }
                if (!response || response.statusCode !== 200) {
                    return reject(response
                        ? this.client.httpError(response)
                        : new Error('No response received'));
                }
                resolve(new deliverabilityReport_js_1.default(body));
            });
        });
    }
}
exports.default = Analysis;
